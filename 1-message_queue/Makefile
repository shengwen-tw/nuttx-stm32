include $(APPDIR)/Make.defs

APP_NAME = mqueue
PRIORITY = SCHED_PRIORITY_DEFAULT
STACKSIZE = 2048

MAINSRC = mqueue_ex.c

CONFIG_MY_APP_PROGNAME ?= mqueue$(EXEEXT)
PROGNAME = $(CONFIG_MY_APP_PROGNAME)

include $(APPDIR)/Application.mk
